info:
  name: projects_suppliers_destroy
  type: http
  seq: 13
  tags:
    - projects

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/projects/:project_pk/suppliers/:id/"
  params:
    - name: id
      value: ""
      type: path
    - name: project_pk
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 Response
    description: No response body
    request:
      url: "{{baseUrl}}/api/v1/projects/:project_pk/suppliers/:id/"
      method: DELETE
      params:
        - name: id
          value: ""
          type: path
        - name: project_pk
          value: ""
          type: path
    response:
      status: 204
      statusText: No Content
      body:
        type: text
        data: ""

docs: Remove a supplier from a project. OWNER only.
