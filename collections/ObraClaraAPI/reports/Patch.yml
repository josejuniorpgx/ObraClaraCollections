info:
  name: Patch
  type: http
  seq: 8
  tags:
  - reports
http:
  method: PATCH
  url: '{{baseUrl}}/api/v1/projects/:project_pk/reports/:id/'
  params:
  - name: project_pk
    value: ''
    type: path
  - name: id
    value: ''
    type: path
    description: UUID del report.
  body:
    type: json
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
examples:
- name: 200 Response
  request:
    url: '{{baseUrl}}/api/v1/projects/:project_pk/reports/:id/'
    method: PATCH
    params:
    - name: project_pk
      value: ''
      type: path
    - name: id
      value: ''
      type: path
    body:
      type: json
      data: |-
        {
          "title": "",
          "visible_to_client": true,
          "status": "published"
        }
  response:
    status: 200
    statusText: OK
    headers:
    - name: Content-Type
      value: application/json
    body:
      type: json
      data: |-
        {
          "title": "",
          "visible_to_client": true,
          "status": "published"
        }
docs: |-
  Update a draft report (title, visibility, status).
  OWNER only. Only draft reports can be modified. Set status to "published" to publish.
script:
  tests: |-
    test("Status is 200", function() {
      expect(res.getStatus()).to.equal(200);
    });
