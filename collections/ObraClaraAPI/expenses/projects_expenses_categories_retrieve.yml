info:
  name: projects_expenses_categories_retrieve
  type: http
  seq: 3
  tags:
    - expenses

http:
  method: GET
  url: "{{baseUrl}}/api/v1/projects/:project_pk/expenses/categories/:id/"
  params:
    - name: id
      value: ""
      type: path
    - name: project_pk
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/api/v1/projects/:project_pk/expenses/categories/:id/"
      method: GET
      params:
        - name: id
          value: ""
          type: path
        - name: project_pk
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": "",
            "name": "",
            "image": ""
          }

docs: Retrieve, update, or delete an expense category.
