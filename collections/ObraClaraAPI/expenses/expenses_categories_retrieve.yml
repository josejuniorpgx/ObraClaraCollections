info:
  name: expenses_categories_retrieve
  type: http
  seq: 15
  tags:
    - expenses

http:
  method: GET
  url: "{{baseUrl}}/api/v1/expenses/categories/"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: No response body
    request:
      url: "{{baseUrl}}/api/v1/expenses/categories/"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

docs: Return distinct category names across all projects where the user participates.
