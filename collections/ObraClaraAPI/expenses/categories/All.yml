info:
  name: All
  type: http
  seq: 1
  tags:
  - expenses
http:
  method: GET
  url: '{{baseUrl}}/api/v1/expenses/categories/'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
examples:
- name: 200 Response
  description: No response body
  request:
    url: '{{baseUrl}}/api/v1/expenses/categories/'
    method: GET
  response:
    status: 200
    statusText: OK
    body:
      type: text
      data: ''
docs: Return distinct category names across all projects where the user participates.
script:
  tests: |-
    test("Status is 200", function() {
      expect(res.getStatus()).to.equal(200);
    });

    test("Response is JSON", function() {
      const contentType = res.getHeader("Content-Type");
      expect(contentType).to.contain("application/json");
    });
