info:
  name: invitations_accept_create
  type: http
  seq: 5
  tags:
    - invitations

http:
  method: POST
  url: "{{baseUrl}}/api/v1/invitations/:token/accept/"
  params:
    - name: token
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: No response body
    request:
      url: "{{baseUrl}}/api/v1/invitations/:token/accept/"
      method: POST
      params:
        - name: token
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""

docs: Accept an invitation. Must be authenticated.
