info:
  name: projects_invitations_destroy
  type: http
  seq: 3
  tags:
    - invitations

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/projects/:project_pk/invitations/:id/"
  params:
    - name: id
      value: ""
      type: path
    - name: project_pk
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 Response
    description: No response body
    request:
      url: "{{baseUrl}}/api/v1/projects/:project_pk/invitations/:id/"
      method: DELETE
      params:
        - name: id
          value: ""
          type: path
        - name: project_pk
          value: ""
          type: path
    response:
      status: 204
      statusText: No Content
      body:
        type: text
        data: ""

docs: Cancel a pending invitation. OWNER only.
