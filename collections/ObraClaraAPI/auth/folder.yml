info:
  name: auth
  type: folder
  seq: 1

request:
  auth: inherit

docs: |-
  Authentication and user management (django-allauth + dj-rest-auth + simplejwt).
  Email-based authentication. JWT access/refresh tokens.
  Includes login, register, password reset/change, email verification, and profile management.
