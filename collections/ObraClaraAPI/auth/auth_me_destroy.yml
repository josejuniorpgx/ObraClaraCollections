info:
  name: auth_me_destroy
  type: http
  seq: 17
  tags:
    - auth

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/auth/me/"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 Response
    description: No response body
    request:
      url: "{{baseUrl}}/api/v1/auth/me/"
      method: DELETE
    response:
      status: 204
      statusText: No Content
      body:
        type: text
        data: ""
