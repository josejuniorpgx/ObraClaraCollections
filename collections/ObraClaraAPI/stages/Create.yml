info:
  name: Create
  type: http
  seq: 3
  tags:
  - stages
http:
  method: POST
  url: '{{baseUrl}}/api/v1/projects/:project_pk/stages/'
  params:
  - name: project_pk
    value: ''
    type: path
  body:
    type: json
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
examples:
- name: 201 Response
  request:
    url: '{{baseUrl}}/api/v1/projects/:project_pk/stages/'
    method: POST
    params:
    - name: project_pk
      value: ''
      type: path
    body:
      type: json
      data: |-
        {
          "name": "",
          "percentage": "",
          "budget_estimated": "",
          "order": 0
        }
  response:
    status: 201
    statusText: Created
    headers:
    - name: Content-Type
      value: application/json
    body:
      type: json
      data: |-
        {
          "name": "",
          "percentage": "",
          "budget_estimated": "",
          "order": 0
        }
docs: CRUD for stages scoped to a project.
script:
  post-response: |-
    const body = res.getBody();
    if (body.id) {
      bru.setEnvVar("stage_id", body.id);
    }
