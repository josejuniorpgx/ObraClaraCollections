info:
  name: Get
  type: http
  seq: 4
  tags:
  - stages
http:
  method: GET
  url: '{{baseUrl}}/api/v1/projects/:project_pk/stages/:id/'
  params:
  - name: id
    value: ''
    type: path
    description: Un Cadena UUID que identifique este stage.
  - name: project_pk
    value: ''
    type: path
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
examples:
- name: 200 Response
  request:
    url: '{{baseUrl}}/api/v1/projects/:project_pk/stages/:id/'
    method: GET
    params:
    - name: id
      value: ''
      type: path
      description: Un Cadena UUID que identifique este stage.
    - name: project_pk
      value: ''
      type: path
  response:
    status: 200
    statusText: OK
    headers:
    - name: Content-Type
      value: application/json
    body:
      type: json
      data: |-
        {
          "id": "",
          "name": "",
          "percentage": "",
          "budget_estimated": "",
          "order": 0,
          "total_progress": "",
          "total_spent": "",
          "created": "",
          "modified": ""
        }
docs: CRUD for stages scoped to a project.
script:
  tests: |-
    test("Status is 200", function() {
      expect(res.getStatus()).to.equal(200);
    });

    test("Response is JSON", function() {
      const contentType = res.getHeader("Content-Type");
      expect(contentType).to.contain("application/json");
    });
