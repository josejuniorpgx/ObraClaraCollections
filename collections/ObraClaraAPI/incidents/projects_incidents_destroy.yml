info:
  name: projects_incidents_destroy
  type: http
  seq: 4
  tags:
    - incidents

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/projects/:project_pk/incidents/:id/"
  params:
    - name: id
      value: ""
      type: path
      description: Un Cadena UUID que identifique este incident.
    - name: project_pk
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 Response
    description: No response body
    request:
      url: "{{baseUrl}}/api/v1/projects/:project_pk/incidents/:id/"
      method: DELETE
      params:
        - name: id
          value: ""
          type: path
          description: Un Cadena UUID que identifique este incident.
        - name: project_pk
          value: ""
          type: path
    response:
      status: 204
      statusText: No Content
      body:
        type: text
        data: ""

docs: CRUD for incidents scoped to a project.
