info:
  name: mediafiles
  type: folder
  seq: 9

request:
  auth: inherit

docs: |-
  Media file management for projects. File types: image, document (20MB limit).
  Can be attached to: progress entries, expenses, incidents, follow-ups.
  Source type is computed from FK relationships.
  Includes document-only filter for unlinked project documents.
